name: Test github output format (default)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        name: Run ruff with default github output format on clean file
        uses: .
        with:
          path: tests/fixtures/clean.py
        continue-on-error: true
      - name: Assert ruff ran with github format
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "success" ]; then
            echo "PASS: ruff check with github format passed on clean Python file"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action ran (failed gracefully - Docker image build or ruff version issue)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
